# Convex Configuration
VITE_CONVEX_URL=your_convex_url_here

# Convex Auth Configuration
# Generate JWT keys with: node generateKeys.mjs (see AUTHENTICATION.md)
# Set these in Convex Dashboard → Settings → Environment Variables:
# JWT_PRIVATE_KEY=your_jwt_private_key_here
# JWKS=your_jwks_here
# SITE_URL=http://localhost:5173 (optional, only needed for OAuth)

# Sentry Configuration
# Get your DSN from https://sentry.io/settings/[org]/projects/[project]/keys/
VITE_SENTRY_DSN=your_sentry_dsn_here

# Sentry Source Maps Upload (for production builds)
# Only needed for CI/CD or production builds
# Get these from https://sentry.io/settings/account/api/auth-tokens/
SENTRY_ORG=your_sentry_org
SENTRY_PROJECT=your_sentry_project
SENTRY_AUTH_TOKEN=your_sentry_auth_token

# Firecrawl Configuration
# Get your API key from https://www.firecrawl.dev/
# Set this in Convex Dashboard → Settings → Environment Variables:
# FIRECRAWL_API_KEY=your_firecrawl_api_key_here

# AI Configuration (for restaurant data enrichment)
# Set these in Convex Dashboard → Settings → Environment Variables:
# AI_API_KEY=your_openai_api_key_here (or use OPENAI_API_KEY)
# AI_BASE_URL=https://api.openai.com/v1 (optional, for OpenAI-compatible providers)
# AI_MODEL=gpt-4o-mini (optional, defaults to gpt-4o-mini)

